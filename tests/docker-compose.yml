services:
  test_db:
    image: postgres
    environment:
      - POSTGRES_USER=dev_user
      - POSTGRES_PASSWORD=secure_password
      - POSTGRES_DB=mycommercedb
    ports:
      - 5432:5432
    volumes:
      - ./db/dump.sql:/docker-entrypoint-initdb.d/create_tables.sql